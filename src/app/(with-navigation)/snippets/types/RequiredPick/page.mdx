import CodeBlock from "@/components/CodeBlock";

# RequiredPick

by default, the Required makes everything required, but you can use this type to make a required pick of the type

<CodeBlock path="src/app/(with-navigation)/snippets/types/RequiredPick/index.ts" />
