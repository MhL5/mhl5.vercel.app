import CodeBlock from "@/components/CodeBlock";

# PartialPick

by default, the partial makes everything optional, but you can use this type to make a partial pick of the type

<CodeBlock path="src/app/(with-navigation)/snippets/types/PartialPick/index.ts" />
