import CodeBlock from "@/components/CodeBlock";

# AllOrNothing

a generic type that allows you to either pass in all the properties or none of them

<CodeBlockServer path="src/app/(with-navigation)/snippets/types/AllOrNothing/index.ts" />
