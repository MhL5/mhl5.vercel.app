# ScrollToTopOnNavigation

A simple component that scrolls to the top of the page when the pathname changes.

<CodeBlock path="src/app/(with-navigation)/snippets/components/ScrollToTopOnNavigation/index.tsx" />
