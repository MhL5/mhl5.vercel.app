import InputWithIcon from "@/app/(with-navigation)/snippets/components/InputWithIcon";
import { Search } from "lucide-react";
import CodeBlockServer from "@/components/code-components/CodeBlock/CodeBlockServer";
import CodePreview from "@/components/code-components/CodePreview";
import Example from "@/app/(with-navigation)/snippets/components/InputWithIcon/Example";

<SnippetH1 heading={`InputWithIcon`} pageSlug="InputWithIcon" />

The `InputWithIcon` component is a customizable input field that supports icons on either the left or right side. It's built on top of the base `Input` component.

Known issues:

- Changing the container's padding will break the positioning

<CodeBlockServer path="src/app/(with-navigation)/snippets/components/InputWithIcon/index.tsx" />

## Examples

<CodePreview
  preview={<Example />}
  codePath="src/app/(with-navigation)/snippets/components/InputWithIcon/Example.tsx"
/>
