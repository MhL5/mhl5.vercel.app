import { TriangleAlert } from "lucide-react";
import { Alert, AlertDescription, AlertTitle } from "@/components/ui/alert";

<SnippetH1 heading="writeFile" pageSlug="writeFile" />

a server action that writes data to a temporary file with a unique filename.

This action is only available in Node.js runtime.

<Alert variant="warning" className="not-prose">
  <TriangleAlert />
  <AlertTitle className="mb-1.5">
    This action is only available in Node.js runtime.
  </AlertTitle>
  <AlertDescription>
    This action is only available in Node.js runtime. if you are using this
    action in Edge runtime, it will throw an error.
  </AlertDescription>
</Alert>

## Snippet

<CodeBlockServer path="src/app/(with-navigation)/snippets/actions/writeFile/index.ts" />
