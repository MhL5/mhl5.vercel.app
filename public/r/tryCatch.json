{
  "$schema": "https://ui.shadcn.com/schema/registry-item.json",
  "name": "tryCatch",
  "type": "registry:lib",
  "title": "tryCatch",
  "description": "utility function for catching errors, it aims to replace `try-catch` blocks. it returns a tuple of `[error, data]` and its fully type safe.",
  "files": [
    {
      "path": "src/registry/utils/tryCatch/tryCatch.ts",
      "content": "type Result<T, E> = [undefined, T] | [E, undefined];\r\n\r\nexport async function tryCatch<T, E = Error>(\r\n  promise: Promise<T>,\r\n): Promise<Result<T, E>> {\r\n  try {\r\n    return [undefined, await promise];\r\n  } catch (error) {\r\n    return [error as E, undefined];\r\n  }\r\n}\r\n",
      "type": "registry:lib",
      "target": "src/utils/tryCatch.ts"
    }
  ]
}