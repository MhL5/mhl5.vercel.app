You can customize the fallback UI by passing a `fallback` prop to the
`ErrorBoundary` component.

<Alert variant="info" className="not-prose">
  <AlertDescription>
    in `Next.Js 16.3` or newer you should use
    [catchError](https://nextjs.org/docs/app/api-reference/functions/catchError)
  </AlertDescription>
</Alert>

<br />

<PreviewCode name="ErrorBoundary" height="lg" />

## Installation

<InstallationTabs name="ErrorBoundary" />

## Usage

```tsx
import ErrorBoundary from "@/components/ui/ErrorBoundary";
```

```tsx
<ErrorBoundary>
  <Component />
</ErrorBoundary>
```
