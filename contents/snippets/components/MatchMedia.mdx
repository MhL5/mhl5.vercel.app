<Alert variant="warning" className="not-prose">
  <AlertTitle>
  Heads up!
  </AlertTitle>
  <AlertDescription>
     If you are using React 19.2 or above, you should use [Activity](https://react.dev/reference/react/Activity) instead. 
  </AlertDescription>
</Alert>

It uses a custom [useMediaQuery](/snippets/hooks/useMediaQuery) hook internally to determine whether the
specified media conditions are met.

<CodePreview name="MatchMedia" />

## Installation

<InstallationTabs name="MatchMedia" />

## Usage

```tsx
import { MatchMediaBreakpoint, MatchMedia } from "@/components/ui/MatchMedia";
```

<br />

```tsx
<MatchMediaBreakpoint breakpoint="sm">
  <div>children&apos;s gets rendered on sm tailwindcss breakpoint</div>
</MatchMediaBreakpoint>

<MatchMedia query="(max-width:640px)" fallback={<div>fallback</div>}>
  <div>children&apos;s gets rendered on max-width:640px</div>
</MatchMedia>
```
