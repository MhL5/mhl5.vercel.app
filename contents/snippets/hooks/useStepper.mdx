<PreviewCode name="useStepper" />

## Installation

<InstallationTabs name="useStepper" />

## Usage

```tsx
import { useStepper } from "@/hooks/useStepper";
```

<br />

```tsx
const { back, currentStep, goTo, next, canGoBack, canGoNext, canGoTo } = useStepper(steps);
```
