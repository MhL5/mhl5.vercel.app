import Example from "@/app/(with-navigation)/_snippets/components/DebouncedInput/Example";

<SnippetH1 heading="useDebounce" />

Simple hook for debouncing a value. depending on the use case, you can use the `useDebounce` hook or the `useDebounceValue` hook.

<CodePreview
  preview={<Example />}
  codePath="src/app/(with-navigation)/snippets/components/DebouncedInput/Example.tsx"
/>

<ComponentSource path="src/app/(with-navigation)/snippets/hooks/useDebounce/index.ts" />
