<SnippetH1 heading="useTimeout" />

its a Hook that returns a tuple with the reset, clear functions. the callback is called when the timeout is finished.

<ComponentSource path="src/app/(with-navigation)/snippets/hooks/useTimeout/index.ts" />
