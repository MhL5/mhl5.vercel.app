<SnippetH1 heading="useIsVisible" />

Simple hook for checking if an element is visible in the viewport using the **Intersection Observer API**.

<ComponentSource path="src/app/(with-navigation)/snippets/hooks/useIsVisible/index.ts" />
