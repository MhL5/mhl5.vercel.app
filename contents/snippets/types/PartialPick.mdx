by default, the partial makes everything optional, but you can use this type to make a partial pick of a type.

<ComponentSource path="src/app/(with-navigation)/snippets/types/PartialPick/index.ts" />
