by default, the Required makes everything required, but you can use this type to make a required pick of a type.

<ComponentSource path="src/app/(with-navigation)/snippets/types/RequiredPick/index.ts" />
